<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.friday.peanutbutter.mapper.ThreadExtMapper">
    <resultMap id="BaseResultMap" type="com.friday.peanutbutter.model.PostThread">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Thu May 05 22:50:49 CST 2022.
        -->
        <id column="id" jdbcType="BIGINT" property="id" />
        <result column="title" jdbcType="VARCHAR" property="title" />
        <result column="gmt_create" jdbcType="BIGINT" property="gmtCreate" />
        <result column="gmt_modified" jdbcType="BIGINT" property="gmtModified" />
        <result column="creator" jdbcType="INTEGER" property="creator" />
        <result column="like_count" jdbcType="INTEGER" property="likeCount" />
        <result column="comment_count" jdbcType="INTEGER" property="commentCount" />
        <result column="view_count" jdbcType="INTEGER" property="viewCount" />
        <result column="tag" jdbcType="VARCHAR" property="tag" />
    </resultMap>
    <update id="incView" parameterType="com.friday.peanutbutter.model.PostThread">
        update thread
        set
        view_count = view_count+#{viewCount,jdbcType=INTEGER}
        where id = #{id}
    </update>

    <update id="incCommentCount" parameterType="com.friday.peanutbutter.model.PostThread">
        update thread
        set
        comment_count = comment_count+#{commentCount,jdbcType=INTEGER}
        where id = #{id}
    </update>

    <select id="selectRelated" parameterType="com.friday.peanutbutter.model.PostThread" resultMap="BaseResultMap">
    <!--以正则的方式匹配相关帖子 -->
        select  * from thread where id!=#{id} and tag regexp #{tag}
    </select>

    <select id="selectBySearch" parameterType="String" resultMap="BaseResultMap">
        select * from thread
        <where>
            <if test="search != null">
                and title regexp #{search}
            </if>
        </where>
        order by gmt_create desc
    </select>

</mapper>